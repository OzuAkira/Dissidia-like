using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DOWN_nextPage : menuScript
{
    /*
    public GameObject _headList;
    public GameObject _centerList;
    public GameObject _tailList;
    public int fps = 60;
    private GameObject _;
    */
    public override void select()
    {

    }
    /*
    IEnumerator ChangePage()
    {
        _headList.transform.position = _tailList.transform.position;

        for (int i = 0; i < fps; i++)
        {
            _centerList.transform.position += new Vector3(0, 8, 0);
            _tailList.transform.position += new Vector3(0, 8, 0);
            yield return null;
        }
        _headList =
    */

}
